# Hugging Face API key for chat inference.
# Get a token at: https://huggingface.co/settings/tokens
# Copy this file to .env and replace with your actual key.
# Do NOT commit .env to version control.
#
# Why VITE_ prefix is required:
# Vite only exposes env vars to the client bundle when they start with VITE_.
# This prevents accidentally leaking server-side secrets into the frontend.

VITE_HUGGINGFACE_API_KEY=your_actual_huggingface_api_key_here
